import { type ReactElement } from "react";
import { Text } from "@gluestack-ui/themed";

export function ExpandedTodo(): ReactElement {
  // rows :
  // 1. checkbox + content
  // 2. Bell icon + Date + remove icon
  // 3. add to DB
  // 4. delete tab
  return <Text>Hi from todo with id : </Text>;
}
